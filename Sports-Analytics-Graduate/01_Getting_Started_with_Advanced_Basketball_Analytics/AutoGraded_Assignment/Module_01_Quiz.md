# Module 01 Quiz (Auto-Graded, D2L)

Short, definition-level check of this module's key terms. 7 questions, 1 point each.

1. (Multiple choice) `pacman::p_load()` is used to:
   a. Visualize a network graph
   b. Install a package only if missing, then load it — in one call
   c. Resolve function-name conflicts between packages
   d. Convert play-by-play data into a usable format

2. (Multiple choice) `conflicted::conflict_prefer("filter", "dplyr")` is used to:
   a. Remove rows that don't meet a condition
   b. Force `filter()` to always resolve to the `dplyr` version when more than one package defines it
   c. Install the `dplyr` package
   d. Filter play-by-play data to a single team

3. (Multiple choice) In `BasketballAnalyzeR`, which function turns raw play-by-play data into the format `assistnet()` needs?
   a. `fourfactors()`
   b. `PbPmanipulation()`
   c. `expectedpts()`
   d. `hclustering()`

4. (Short answer) In one sentence, what does an assist network show that a season points-per-game leaderboard does not?

5. (Multiple choice) In the 2017-18 Golden State Warriors assist network built in class, which player had the most total assists?
   a. Stephen Curry
   b. Kevin Durant
   c. Draymond Green
   d. Klay Thompson

6. (Multiple choice) This course's graduate-level track is built around:
   a. Fantasy basketball scoring
   b. Injury-risk detection
   c. Ticket pricing models
   d. Referee bias analysis

7. (True/False) This course restricts itself entirely to the assigned textbook and does not draw on outside journal articles.
