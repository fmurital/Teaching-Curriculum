# Reproducibility

Per ACLOS's reproducibility standard: every worked example and figure built for this course is run for real, not fabricated, and its R source is saved in the relevant module's `Coding_Exercise/` (or `Lab_Activity/`) folder so students can rerun it themselves. This folder documents the shared environment (R version, package versions) once Module 1 locks it in.
