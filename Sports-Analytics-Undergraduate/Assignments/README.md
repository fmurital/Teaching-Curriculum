# Assignments

Standalone, graded assignments that span more than one module (as opposed to the per-module `AutoGraded_Assignment/` folder inside each module). Empty until the first assignment is built.
